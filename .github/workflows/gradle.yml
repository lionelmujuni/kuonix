name: Build, Test, and Generate Doxygen Docs

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  # ----- Gradle Build & Test  -----
  gradle-test:
    name: Gradle Build & Test
    runs-on: ubuntu-latest

    container:
      image: gradle:8.14.3-jdk24
      # Gradle cache is handled below via the setup-gradle action

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Cache Gradle dependencies
      - name: Setup Gradle (with caching)
        uses: gradle/actions/setup-gradle@v4
        with:
          validate-wrappers: false

      - name: Display Gradle and Java versions
        run: |
          gradle --version
          java -version

      - name: Run tests
        working-directory: restful   
        run: bash ./gradlew test

  # ----- Generate Doxygen Docs  -----
  generate-doxygen-docs:
    name: Generate Doxygen Docs
    runs-on: ubuntu-latest

    # Same lightweight Debian image used in Bitbucket
    container:
      image: debian:bullseye-slim

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Doxygen and Graphviz
        run: |
          apt-get update
          apt-get install -y doxygen graphviz

      - name: Run Doxygen
        run: doxygen Doxyfile

      # Upload the generated HTML docs as a workflow artifact
      - name: Upload Doxygen documentation
        uses: actions/upload-artifact@v4
        with:
          name: doxygen-docs
          path: docs/html/**
